	uint32_t brightness = 0;
	uint8_t direction = 1;

  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1)
  {
	  if (direction)
	  {
		  brightness++;
		  if (brightness >= 1000) direction = 0;
	  }
	  else
	  {
		  if (brightness > 0) brightness--;
		  else direction = 1;
	  }
	  __HAL_TIM_SET_COMPARE(&htim2, TIM_CHANNEL_4, 999-brightness);
	  HAL_Delay(2);